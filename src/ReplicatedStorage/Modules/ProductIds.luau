--!strict
-- Roblox Developer Product IDs (repeatable purchases, unlike Gamepasses which are one-time).
-- Fill these in after creating each product in the Creator Dashboard. 0 = not configured yet.

return {
	GroupColorChange = 0, -- 20 Robux, charged every time the group color is changed
}
